import { AdditionType, HitsoundType } from "./structs";
export default class Hitsound {
    Sample: HitsoundType;
    Additions: AdditionType;
    CustomSampleIndex: number;
    constructor(Sample: HitsoundType, Additions: AdditionType);
    static parse(line: string): Hitsound;
}
