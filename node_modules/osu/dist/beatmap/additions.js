"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const errors_1 = require("../errors");
class Hitsound {
    constructor(Sample, Additions) {
        this.Sample = Sample;
        this.Additions = Additions;
        this.CustomSampleIndex = 0;
    }
    static parse(line) {
        let parts = line.split(":");
        let sample = null;
        let addition = null;
        if (parts[0] && parts[0] !== "0")
            sample = parseInt(parts[0]);
        if (sample == null || addition == null)
            throw new errors_1.ParseError(`Invalid hitsound: '${line}'`);
        return new Hitsound(sample, addition);
    }
}
exports.default = Hitsound;
//# sourceMappingURL=additions.js.map