import Color from "../color";
import { HitObject } from "./hitObject";
import { BeatmapConfig, Difficulty } from "./structs";
import TimingPoint from "./timingPoint";
export declare class Beatmap {
    FileFormat: number;
    Config: BeatmapConfig;
    BpmMin: number;
    BpmMax: number;
    Difficulty: Difficulty;
    Bookmarks: number[];
    HitObjects: HitObject[];
    TimingPoints: TimingPoint[];
    MaxCombo: number;
    ReactionTime: number;
    ComboColors: Color[];
    Tags: string[];
    constructor(FileFormat: number, Config: BeatmapConfig, BpmMin: number, BpmMax: number, Difficulty: Difficulty, Bookmarks: number[], HitObjects: HitObject[], TimingPoints: TimingPoint[]);
    static parse(file: string): Promise<Beatmap>;
    static parseString(data: string): Promise<Beatmap>;
    UpdateStacking(start?: number, end?: number): void;
    GetTimingPoint(offset: number): TimingPoint;
    GetIndexAt(offset: number): number;
}
