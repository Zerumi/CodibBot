export default class Color {
    red: number;
    green: number;
    blue: number;
    constructor(red: number, green: number, blue: number);
    static hex(red: number, green: number, blue: number): string;
    static fromArray(colors: number[]): Color;
    hex(): string;
    clone(): Color;
    toArray(): number[];
}
