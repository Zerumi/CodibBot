import Vector from "./vector";
export declare class Spline {
    control: Vector[];
    length: number;
    points: Vector[];
    private pxLength;
    private lengthMap;
    constructor(control: Vector[], length: number);
    static bezier(control: Vector[], length: number): Spline;
    static linear(control: Vector[], length: number): Spline;
    static perfect(control: Vector[], length: number): Spline;
    calculate(): void;
    pointAt(progress: number): Vector;
}
export declare class BezierSpline extends Spline {
    constructor(points: Vector[], length: number);
}
export declare class LinearSpline extends Spline {
    constructor(points: Vector[], length: number);
}
export declare class PerfectSpline extends Spline {
    constructor(points: Vector[], length: number);
}
